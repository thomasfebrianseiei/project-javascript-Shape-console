// The formula of rectangle area is
// Area = width * length
const rectangleWidth = 100
const rectangleLength = 20
const number1=2
const number2=5

const rectangleArea = rectangleWidth * rectangleLength
const pangkat= number1 ** number2
const akar= pangkat**0.5


console.log(`${rectangleArea} = ${rectangleWidth} * ${rectangleLength} true`)
console.log(`${pangkat} = ${number1} pangkat ${number2} true`)
console.log(`${akar} = akar ${pangkat} true`)

